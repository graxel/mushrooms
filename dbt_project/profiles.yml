mushrooms:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('PG_HOST') }}"
      user: "{{ env_var('PG_USER') }}"
      password: "{{ env_var('PG_PASS') }}"
      port: "{{ env_var('PG_PORT') | int }}"
      dbname: mushrooms_dev
      schema: "{{ env_var('USER') }}"
    qa:
      type: postgres
      host: "{{ env_var('PG_HOST') }}"
      user: "{{ env_var('PG_USER') }}"
      password: "{{ env_var('PG_PASS') }}"
      port: "{{ env_var('PG_PORT') | int }}"
      dbname: mushrooms_qa
      schema: fakeschema
    prod:
      type: postgres
      host: "{{ env_var('PG_HOST') }}"
      user: "{{ env_var('PG_USER') }}"
      password: "{{ env_var('PG_PASS') }}"
      port: "{{ env_var('PG_PORT') | int }}"
      dbname: mushrooms
      schema: fakeschema
